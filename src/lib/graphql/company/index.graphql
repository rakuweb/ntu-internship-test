query getRecruiterById($id: ID) {
  company(id: $id) {
    data {
      id
      attributes {
       cover_image {
        data {
            id
          attributes {
              url
              width
              height
              alternativeText
            }
          }
        }
        createdBy {
          id
          firstname
          lastname
        }
      }
    }
  }
}
