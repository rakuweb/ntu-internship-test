query getJobCategories {
  jobCategories(pagination: { limit: 1000 }) {
    data {
      id
      attributes {
        name
        en_name
        offers {
          data {
            id
          }
        }
      }
    }
  }
}
