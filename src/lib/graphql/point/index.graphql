query getPoints {
  points(pagination: { limit: 1000 }) {
    data {
      id
      attributes {
        name
        offer {
          data {
            id
          }
        }
      }
    }
  }
}
